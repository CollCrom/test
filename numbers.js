// old
var a = 1;

// new
//const are immutable, they cannot change.
const a = 1;

//let
// let you can change. It is mutable. Use case is loops
let b = 2;

const multiple = 9 * 9;
const divine = 9 / 9;
const addition = 9 + 9;
const subtraction = 9 - 9;
const mod = 9 % 9;

//variable names should be camelCase
const randNumber = Math.random();

const zeroToTen = Math.floor(Math.random() * 10); 

// typeof
// checks the data type
typeof("cat");

//whole # ex. 8 is an int
//dec # ex 8.8 is a float